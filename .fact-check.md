# 外部事實查證紀錄（asp-fact-verify）

> 查證日期：2026-06-17
> 範圍：slidev-deck-stack skill 所引用的 Slidev API / 語法 / 匯出旗標 / 套件版本
> 唯一權威來源：https://sli.dev 官方文件 + npm registry

| # | 主張 | 查證結果 | 來源 |
|---|------|---------|------|
| 1 | Slidev 現行主版為 v52，`@slidev/cli` = 52.16.0；scaffold 指令 `npm init slidev`（= `npm create slidev`） | ✅ 確認（npm registry，約 12 天前發布）；既有專案 `/home/ubuntu/slide` 亦鎖 `^52.16.0` | [npm @slidev/cli](https://www.npmjs.com/package/@slidev/cli)、`npm view @slidev/cli version` |
| 2 | 第一個 `---` 區塊＝headmatter（全域）、其後每頁 `---`＝per-slide frontmatter；分隔線前後需空行；`src:` 可匯入外部 md | ✅ 確認 | [sli.dev/guide/syntax](https://sli.dev/guide/syntax) |
| 3 | `<v-clicks>`（支援 `depth`/`every`）、`v-click` 指令/元件、`v-after`(=`v-click="'+0'"`)、`.hide`/`<v-click hide>`、`$clicks`、絕對 `v-click="3"` vs 相對 `'+1'`/`'-1'`（需引號）、進出區間陣列（結束索引不含） | ✅ 確認 | [sli.dev/guide/animations](https://sli.dev/guide/animations) |
| 4 | Shiki magic-move 用**四個反引號** ` ````md magic-move `，多區塊逐 click morph；可帶 `[檔名]` 標題列、`{duration}`/`magicMoveDuration`(預設 800ms)、區塊內 `{*\|1\|2-5}` 逐步高亮 | ✅ 確認 | [sli.dev/features/shiki-magic-move](https://sli.dev/features/shiki-magic-move) |
| 5 | 行高亮 `\`\`\`ts {2,3\|5\|all}`，`{lines:true}` 顯示行號、`{maxHeight}` 可捲動 | ✅ 確認（同 magic-move 頁與 syntax 範例一致） | [sli.dev/features/shiki-magic-move](https://sli.dev/features/shiki-magic-move) |
| 6 | 內建 layout 全表（center/cover/default/end/fact/full/image(-left/-right)/iframe(-left/-right)/intro/none/quote/section/statement/two-cols/two-cols-header）；`two-cols` 用 `::right::`（前＝左）；`two-cols-header` 用「分隔線前內容＝跨欄 header」+ `::left::`/`::right::` | ✅ 確認，並修正草案：**沒有 `::title::` 具名 slot** | [sli.dev/builtin/layouts](https://sli.dev/builtin/layouts) |
| 7 | 匯出 `slidev export` 預設 PDF，`--format pptx\|png\|md`；依賴 `playwright-chromium`(dev dep)；逐步點擊用 `--with-clicks`（PPTX 預設開、`--with-clicks false` 關）；`--output/--dark/--range/--timeout/--wait`、headmatter `exportFilename` | ✅ 確認 | [sli.dev/guide/exporting](https://sli.dev/guide/exporting) |
| 8 | `theme:` headmatter（官方主題可省 `slidev-theme-`/`@slidev/theme-` 前綴，scoped 需全名）、`addons:` 陣列、缺套件自動提示安裝 | ✅ 確認 | [sli.dev/guide/theme-addon](https://sli.dev/guide/theme-addon) |
| 9 | `fonts:` headmatter 子鍵 sans/serif/mono/weights/italic/provider(google\|coollabs\|none)/local/fallbacks，預設自 Google Fonts CDN；`local:` 退出自動匯入、`provider:none` 全關 | ✅ 確認。官方文件**對 CJK/中文字型無專節** → 中文策略（限 weights、子集化 woff2、`provider:none` + `@font-face`）為依官方鍵組合的最佳實務，非官方明列 | [sli.dev/custom/config-fonts](https://sli.dev/custom/config-fonts) |
| 10 | `<<<` 匯入：`<<< @/snippets/x.ts`、`#region`、`{2,3\|5}{lines:true}`、可加語言與 `{monaco}`；`@` 指向專案根、建議放 `@/snippets` 以相容 Monaco | ✅ 確認 | [sli.dev/features/import-snippet](https://sli.dev/features/import-snippet) |
| 11 | KaTeX：行內 `$…$`、區塊 `$$…$$`、區塊可 `$$ {1\|3\|all}` 逐行高亮；mhchem 需 `import 'katex/contrib/mhchem'`（vite.config）後用 `\ce{...}` | ✅ 確認 | [sli.dev/features/latex](https://sli.dev/features/latex) |
| 12 | PlantUML：` ```plantuml ` 包 `@startuml…@enduml`，預設送 www.plantuml.com；改伺服器用 headmatter `plantUmlServer`。**無 `{scale}` 行內選項**（Mermaid 才有） | ✅ 確認，並修正草案：PlantUML 不吃 `{scale}` | [sli.dev/features/plantuml](https://sli.dev/features/plantuml) |
| 13 | Mermaid：` ```mermaid {theme:'neutral', scale:0.8} ` 可帶 theme/scale | ✅ 確認 | [sli.dev/features/mermaid](https://sli.dev/features/mermaid) |
| 14 | TwoSlash：` ```ts twoslash ` 渲染型別資訊，`// ^?` 就地顯示推導型別 | ✅ 確認 | [sli.dev/features/twoslash](https://sli.dev/features/twoslash) |
| 15 | Monaco：`{monaco}` 可編輯、`{monaco-run}` 可執行、`{monaco-diff}`（`~~~` 分隔前後）、`{height:'300px'}` | ✅ 確認 | [sli.dev/features/monaco-editor](https://sli.dev/features/monaco-editor) |
| 16 | 換頁 transition 名稱：`fade`/`fade-out`/`slide-left`/`slide-right`/`slide-up`/`slide-down`/`view-transition`；`slide-left \| slide-right` 指定前進/後退；物件 `{forward,backward}` 自訂；headmatter 全域、per-slide 覆寫 | ✅ 確認 | [sli.dev/guide/animations](https://sli.dev/guide/animations)、[frontmatter.ts](https://github.com/slidevjs/slidev/blob/main/packages/types/src/frontmatter.ts) |
| 17 | theme / addon / 工具套件版本 | ✅ npm registry（2026-06-17）：`@slidev/theme-default` 0.25.0、`@slidev/theme-seriph` 0.25.0、`slidev-theme-academic` 2.1.0、`slidev-theme-neversink` 0.4.1、`slidev-theme-geist` 0.8.1、`slidev-addon-python-runner` 0.1.3、`slidev-addon-typst` 1.0.2、`playwright-chromium` 1.61.0、`katex` 0.17.0 | `npm view <pkg> version` |
| 18 | theming-style §1 / handoff.md §3 新增主題清單（借鏡官方 theme gallery）的套件名與官方/社群歸屬 | ✅ npm registry（2026-06-18，逐一查證）：`@slidev/theme-apple-basic` 0.25.1（**官方**，repo slidevjs/themes）、`slidev-theme-frankfurt` 1.0.8、`slidev-theme-penguin` 2.3.1、`slidev-theme-dracula` 0.2.0、`slidev-theme-the-unnamed` 0.0.24、`slidev-theme-nord` 0.0.6（皆社群）；the-unnamed 為 Elio Struyf 之 VS Code 主題移植（洋紅 accent）、nord 為 Nord 配色 | [theme gallery](https://sli.dev/resources/theme-gallery)、`npm view <pkg> version` |

## 未逐一查證、以撰寫當下知識為準的低風險項

- MDC（Comark）行內語法細節（`{.class}`、`![](){width=}`）、`drawings` 手繪註記、presenter
  備註為「該頁最後一個 HTML 註解」、`components/` 檔名即標籤自動註冊——皆為 Slidev 既有
  慣例，未逐字截圖查證。
- UnoCSS 內建 preset（Wind/attributify/icons）細節、`uno.config.ts` 擴充 API 以 UnoCSS
  官方為準。
- 社群套件 `slidev-theme-seriph`（0.0.1，與官方 `@slidev/theme-seriph` 不同）為占位套件，
  本 skill 一律推薦官方 `@slidev/theme-seriph`。
- 若上述 API 有變動，更新對應 `references/` 檔並在此補登查證日期與來源。
